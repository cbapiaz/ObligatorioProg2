@using Dominio
@{
    ViewBag.Title = "Index";
}

<h2>Compras</h2>

<div class="container">
    <div class="row">
        <div class='col-sm-6'>
            <div class="form-group">
                <div class='input-group date' id='datetimepicker1'>
                    <input type='text' class="form-control" />
                    <span class="input-group-addon">
                        <span class="glyphicon glyphicon-calendar"></span>
                    </span>
                </div>
            </div>
        </div>
    </div>
</div>
<!--
<section>
    <h3>Busca el paquete por precio</h3>
    <input type="number" id="fechaInicio" />
    <button id="btnBuscar">Buscar</button>
</section>
    -->
<table class="table">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Id compra</th>
            <th scope="col">Fecha de compra</th>
            <th scope="col">Fecha de vencimiento</th>
            <th scope="col">Paquetes Comprados</th>
            <th scope="col">Cancelada</th>
            <th scope="col">Total Compra</th>

        </tr>
    </thead>
    <tbody>
        @foreach (Compra item in ViewBag.Compras)
        {


        <tr>
            <th scope="row"></th>
            <td>@item.Id</td>
            <td>@item.Fecha </td>
            <td>@item.FechaVencimiento</td>
            <td>@item.GetDetalleCompra()</td>
            <td>@item.Cancelada</td>
            <td>@item.GetTotalCompra()</td>

        </tr>
        }
    </tbody>

</table>

@section scripts {
    <script>
        var loggedIn = '@Session["LoggedIn"]' == 'True';
        if (loggedIn) {
       //     $("navBar").show();
        }
        else {
         /*   $("navBar").hide();
            location = "/Login/Index";
       */ }

            $(function () {
                $('#datetimepicker1').datetimepicker();
            });
    </script>
}